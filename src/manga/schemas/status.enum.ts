export enum StatusEnum {
    ONGOING = 'ongoing',
    COMPLETED = 'completed',
    DROPPED = 'dropped'
}

export enum ApprovalStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
}